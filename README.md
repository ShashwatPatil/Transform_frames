This repo has 2 Python scripts with the basic logic for frame conversion

The libuwb_transform is a C++ library that can be installed locally and helps convert RTLS (currently from pozyx) frames to a floorplan plane (image/map frame).
